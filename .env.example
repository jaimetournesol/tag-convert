# Copy to .env and fill in. The CLI reads env first, then .env, then flags.

# --- Auth (pick one) ---
TAG_EMAIL=
TAG_PASSWORD=
# TAG_TOKEN=          # a TAG session JWT, instead of email/password

# --- Target instance (defaults shown; uncomment to override) ---
# TAG_API_URL=https://tag-api.gentledesert-d3828315.westeurope.azurecontainerapps.io
# TAG_RELAY_URL=https://tag-mcp-relay.gentledesert-d3828315.westeurope.azurecontainerapps.io
